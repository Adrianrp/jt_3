<tr>
				<td class="title" colspan="4"><%= line_item.product.title %></td>
			</tr>
			<tr>
				<td><%= line_item.quantity %> &times;</td>
				<td><%= line_item.product.title %></td>
				<td><%= button_to 'delete', line_item, method: :delete, class: "btn btn-info btn-mini" %></td>
				<td><%= currency_kr(line_item.total_price) %></td>
			</tr>